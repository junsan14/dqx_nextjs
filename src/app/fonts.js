import localFont from "next/font/local";

export const mochiy = localFont({
  src: "../../public/fonts/MochiyPopOne-Regular.ttf",
  display: "swap",
  variable: "--font-mochiy",
});

export const kosugi = localFont({
  src: "../../public/fonts/KosugiMaru-Regular.ttf",
  display: "swap",
  variable: "--font-kosugi",
});
